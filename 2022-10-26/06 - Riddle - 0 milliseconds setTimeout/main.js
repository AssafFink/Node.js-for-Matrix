function paint() {
    document.body.style.backgroundColor = "green";
    setTimeout(() => alert("Page changed to green"), 0);
}

function useIt() {
    setTimeout(()=> imageProcessing(...), 0));
}

function imageProcessing(image) { // one minute

}

